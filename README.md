# QuESt Installer

This repository defines the installation layout and platform-specific
installers for the QuESt platform.

This repo does NOT contain:
- QuESt source code
- Solver source code
- Build pipelines

It defines:
- Installed file layout
- Optional solver components (e.g. GLPK)
- Installer behavior per platform
